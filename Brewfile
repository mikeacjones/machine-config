# Brewfile — `brew bundle` installs everything listed here.
# Run via ./bootstrap.sh, or directly: brew bundle --file Brewfile

# --- CLI tools ---
brew "tmux"        # terminal multiplexer (auto-launched by Alacritty)
brew "uv"          # python package/proj manager
brew "gh"          # github cli
brew "ripgrep"     # fast grep (rg)
brew "hyperfine"   # benchmarking
brew "go"          # go toolchain
brew "duti"        # set default apps by file type (used to make Zed the default editor)
# Rust is installed via rustup in bootstrap.sh (not brew), so it self-manages toolchains.
# brew "git"       # newer git than macOS system git, uncomment if wanted
# brew "fd"
# brew "bat"

# --- GUI apps (casks) ---
cask "alacritty"   # terminal emulator
cask "zed"         # editor

# --- fonts ---
cask "font-iosevka"   # provides "Iosevka Term" family used by alacritty.toml
